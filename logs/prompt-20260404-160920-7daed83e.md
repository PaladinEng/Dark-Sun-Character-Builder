# Prompt Execution Log
project: dark-sun-character-builder
prompt_id: 7daed83e-0a7e-4c2a-b719-cada777680fd
task: dark-sun-character-builder-7daed83e
started: 2026-04-04T15:53:53.089382+00:00
outcome: completed-exit-signal-failed

## Prompt
There are 5 confirmed UI bugs in BuilderClient.tsx to fix. Read BuilderClient.tsx carefully before making any changes. Fix them in order, committing after each one.

## Bug 1 — Standard array stat swap deadlock
When using Standard Array, swapping an ability score is blocked if the target value is already assigned elsewhere, creating a deadlock. The disabled logic on the <select> options prevents any reassignment that would temporarily produce a duplicate. Fix this so users can freely reassign values — remove the per-option disabled constraint and instead validate completeness only at export/sheet time, not per-keystroke. The constraint is that all 6 values must each be used exactly once in the final state, not that every intermediate state must be valid.
Commit: "fix(builder): allow standard array score swapping [ckpt 1]"

## Bug 2 — Equipped weapon selector and inventory are disconnected
This is a two-part bug with a user-visible symptom: "chosen weapon spear not in inventory."

Part A — The equipped weapon/armor/shield selectors draw from the full content pack, not from the player's inventory. Selecting a weapon from this dropdown does NOT add it to inventoryItemIds, so validation then fires EQUIPPED_WEAPON_NOT_IN_INVENTORY. Fix the equipped item selectors so they only show items already present in the player's inventory.

Part B — There is no way to add items to inventory. Add an "Add to inventory" control in the equipment/inventory section that lets the user search and select any item from merged content (filtered by type: weapons, armor, shields, gear) and adds it to inventoryItemIds. The equipped item selectors should then automatically reflect the updated inventory.

The correct flow is: add item to inventory first → then equip it. Make that flow obvious in the UI.
Commit: "fix(builder): fix equipped item selectors and add inventory item picker [ckpt 2]"

## Bug 3 — No character name input
CharacterState has a name field used by PDF export and the print sheet, but BuilderClient has no input for it. Add a character name text input in the identity/header section of the builder that updates state.name.
Commit: "fix(builder): add character name input field [ckpt 3]"

## Bug 4 — Point buy does not enforce budget in the UI
The UI displays remaining points but does not prevent overspending. Score increase controls must be disabled when increasing a score would exceed POINT_BUY_BUDGET (27 points). A user should not be able to construct an over-budget allocation through normal UI interaction — the enforcement must be in the controls, not just in validation output.
Commit: "fix(builder): enforce point buy budget in UI controls [ckpt 4]"

## Bug 5 — Background description is one unformatted text block
The background section renders description as a single blob. Check the actual data shape of background entities first — pdfExport.ts already handles separate companion and familiar fields (companionName, companionType, companionSummary, companionNotes, familiarName, familiarType, familiarSummary, familiarNotes). The builder UI should expose these same fields as separate labeled inputs or display sections rather than one combined text block.
Commit: "fix(builder): split background section into labeled sub-fields [ckpt 5]"

## After all fixes
Run the fast harness:
  pnpm --filter @dark-sun/rules typecheck
  pnpm --filter @dark-sun/rules test:unit
  pnpm sheet:golden
  pnpm sheet:invariants
  pnpm guard:duplicates

Update context/STATUS.md and context/WORKQUEUE.md to reflect what was fixed.

Print FINISHED WORK when done.

## Result
Task completed and committed (2c369bc0) but exit signal failed. This is a known issue — no action needed.

## Git Commit
2c369bc0891bf39321c1184bad079dc9e884ddb7

## CPO Log
/home/paladinrobotics/dev/logs/20260404T155353Z_dark-sun-character-builder-7daed83e.log
