# Work Queue

Ordered, actionable, and short. Keep only near-term items.

## Next Up
1. Switch the local machine shell to Node 24 and rerun `pnpm install`, `pnpm --filter web build`, and `pnpm loop:check` without engine warnings.
2. Decide whether the current Next.js build stabilization should remain a bounded local retry wrapper or be replaced by a deeper framework/config fix once the upstream artifact race is better understood.
3. Decide whether `import:seed:check` should gain a diff/assert mode beyond today's non-destructive import smoke.

## Deferred
1. Full preserver spell-point / Rite of Blood automation.
2. Defiler terrain and conservation mechanics.
3. Dark Sun-specific equipment/content beyond the current retained pack items.
